"""Stand-ins for lanes other developers own.

Rule from `contracts/schemas.md` section 8: a mock must behave like the real
service.  A mock that approves everything is forbidden -- it would hide exactly
the failures these mocks exist to exercise.
"""
