/.venv/
.env

# TryTrust local state — Ed25519 keys, database, witnessed roots
var/

__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# Local key material. Issuer/merchant PEMs live in Secret Manager in GCP and
# in this directory in dev — never in git (decision #15).
/secrets/
*.pem

.DS_Store
