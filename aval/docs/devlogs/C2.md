# Devlog — Workstream C2 · The store (merchant: VuelaYa)

Mission: the merchant verifies the mandate itself — signatures against JWKS,
then the verify endpoint — BEFORE charging. Part of workstream C (order
C1 → C2 → C3 when one person runs all three). Scope:
[`../PLAN-PARALELO.md`](../PLAN-PARALELO.md) §3.1. Entry protocol:
[`README.md`](README.md) — newest first, every PR.

---

## 2026-08-29 — workstream opened at M0 freeze
- **Why:** contracts v1.0 are frozen; this log exists so nobody re-solves what C2 already solved.
- **Decision:** none yet. Starting points: [`../../DECISIONS.md`](../../DECISIONS.md) #6 (merchant verifies crypto itself); endpoints in [`../../contracts/api.yaml`](../../contracts/api.yaml) (`/catalog/offers`, `/checkout/charge`); MCP tools contract in [`../../contracts/schemas.md`](../../contracts/schemas.md) §10.
- **Contracts touched:** none.
- **Tests I own:** contract-test of the 402 path (charge refuses when verify ≠ APPROVED); catalog fixtures including the adversarial descriptions.
- **Open questions:** none.
