# Devlog — Workstream C3 · The brain (agent service)

Mission: an agent that discovers and proposes — structurally unable to pay
outside the gate, resilient to prompt injection. Part of workstream C (order
C1 → C2 → C3 when one person runs all three). Scope:
[`../PLAN-PARALELO.md`](../PLAN-PARALELO.md) §3.1. Entry protocol:
[`README.md`](README.md) — newest first, every PR.

---

## 2026-08-29 — workstream opened at M0 freeze
- **Why:** contracts v1.0 are frozen; this log exists so nobody re-solves what C3 already solved.
- **Decision:** this workstream starts from
  [`../decisions/0016-agent-orchestration-without-langgraph.md`](../decisions/0016-agent-orchestration-without-langgraph.md):
  the orchestrator is our own ~150-line graph — explicit nodes, LLM only in
  `propose`, checkpointing in `agent_runs`, `await_human` resumed by
  escalation resolution. **No agent-framework dependency to add.** Identity
  keys: [`../../DECISIONS.md`](../../DECISIONS.md) #9.
- **Contracts touched:** `schemas.md` §5.4 (`await_human` semantics) and §6
  (`agent_runs` DDL) were updated by that decision at freeze time.
- **Tests I own:** T3 (impersonation), T7 (resume idempotency, with B), T11 (injection suite), T12 (Presidio).
- **Open questions:** none.
